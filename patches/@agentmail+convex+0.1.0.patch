diff --git a/node_modules/@agentmail/convex/dist/component/_generated/server.d.ts b/node_modules/@agentmail/convex/dist/component/_generated/server.d.ts
index bec05e6..27db3bb 100644
--- a/node_modules/@agentmail/convex/dist/component/_generated/server.d.ts
+++ b/node_modules/@agentmail/convex/dist/component/_generated/server.d.ts
@@ -21,6 +21,12 @@ import {
 } from "convex/server";
 import type { DataModel } from "./dataModel.js";

+type Env = {
+    readonly AGENTMAIL_API_KEY: string | undefined;
+    readonly AGENTMAIL_WEBHOOK_SECRET: string | undefined;
+    readonly AGENTMAIL_BASE_URL: string | undefined;
+};
+
 /**
  * Define a query in this Convex app's public API.
  *
@@ -94,6 +100,7 @@ export declare const internalAction: ActionBuilder<DataModel, "internal">;
  * @returns The wrapped function. Import this function from `convex/http.js` and route it to hook it up.
  */
 export declare const httpAction: HttpActionBuilder;
+export declare const env: Env;

 /**
  * A set of services for use within Convex query functions.
diff --git a/node_modules/@agentmail/convex/dist/component/_generated/server.js b/node_modules/@agentmail/convex/dist/component/_generated/server.js
index daf1c86..b340067 100644
--- a/node_modules/@agentmail/convex/dist/component/_generated/server.js
+++ b/node_modules/@agentmail/convex/dist/component/_generated/server.js
@@ -75,4 +75,5 @@ export const internalAction = internalActionGeneric;
  * @returns The wrapped function. Import this function from `convex/http.js` and route it to hook it up.
  */
 export const httpAction = httpActionGeneric;
+export const env = process.env;
 //# sourceMappingURL=server.js.map
diff --git a/node_modules/@agentmail/convex/dist/component/convex.config.d.ts b/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
index 08c43c3..94d8b56 100644
--- a/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
+++ b/node_modules/@agentmail/convex/dist/component/convex.config.d.ts
@@ -1,3 +1,7 @@
-declare const component: import("convex/server").ComponentDefinition<any>;
+declare const component: import("convex/server").ComponentDefinition<any, {
+    readonly AGENTMAIL_API_KEY: import("convex/values").VString<string | undefined, "optional">;
+    readonly AGENTMAIL_WEBHOOK_SECRET: import("convex/values").VString<string | undefined, "optional">;
+    readonly AGENTMAIL_BASE_URL: import("convex/values").VString<string | undefined, "optional">;
+}>;
 export default component;
 //# sourceMappingURL=convex.config.d.ts.map
diff --git a/node_modules/@agentmail/convex/dist/component/convex.config.js b/node_modules/@agentmail/convex/dist/component/convex.config.js
index 3d15ed6..76775e6 100644
--- a/node_modules/@agentmail/convex/dist/component/convex.config.js
+++ b/node_modules/@agentmail/convex/dist/component/convex.config.js
@@ -1,6 +1,13 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";
-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+    env: {
+        AGENTMAIL_API_KEY: v.optional(v.string()),
+        AGENTMAIL_WEBHOOK_SECRET: v.optional(v.string()),
+        AGENTMAIL_BASE_URL: v.optional(v.string()),
+    },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });
 export default component;
diff --git a/node_modules/@agentmail/convex/dist/component/utils.js b/node_modules/@agentmail/convex/dist/component/utils.js
index 47ab5db..c3bff1b 100644
--- a/node_modules/@agentmail/convex/dist/component/utils.js
+++ b/node_modules/@agentmail/convex/dist/component/utils.js
@@ -1,3 +1,4 @@
+import { env } from "./_generated/server.js";
 const PERMANENT_STATUSES = new Set([
     400, 401, 404, 405, 410, 413, 414, 415, 422,
 ]);
@@ -5,6 +6,17 @@ const DEFAULT_BASE_URL = "https://api.agentmail.to/v0";
 // Cap stored error bodies so a CDN's HTML 502 page can't balloon a
 // Convex document.
 const ERROR_BODY_LIMIT = 4096;
+function requireApiKey() {
+    const apiKey = env.AGENTMAIL_API_KEY?.trim();
+    if (!apiKey) {
+        throw new Error("AGENTMAIL_API_KEY is not set for the AgentMail component. " +
+            "Bind it through app.use(agentmail, {env: {...}}).");
+    }
+    if (apiKey.length > 512 || !/^[\x21-\x7e]+$/.test(apiKey)) {
+        throw new Error("AGENTMAIL_API_KEY contains invalid HTTP header characters.");
+    }
+    return apiKey;
+}
 /**
  * Parse an ISO timestamp into ms-since-epoch. Returns `Date.now()` on
  * malformed input — `Date.parse` returns `NaN` for bad strings, and `NaN`
@@ -28,12 +40,9 @@ export class AgentMailApiError extends Error {
     }
 }
 export async function agentmailFetch(path, opts) {
-    const apiKey = process.env.AGENTMAIL_API_KEY;
-    if (!apiKey) {
-        throw new Error("AGENTMAIL_API_KEY is not set on this Convex deployment. Run " +
-            "`npx convex env set AGENTMAIL_API_KEY <key>`.");
-    }
-    const baseUrl = (process.env.AGENTMAIL_BASE_URL ?? DEFAULT_BASE_URL).replace(/\/$/, "");
+    const apiKey = requireApiKey();
+    const configuredBaseUrl = env.AGENTMAIL_BASE_URL?.trim();
+    const baseUrl = (configuredBaseUrl || DEFAULT_BASE_URL).replace(/\/+$/, "");
     const url = new URL(baseUrl + path);
     if (opts.query) {
         for (const [k, v] of Object.entries(opts.query)) {
diff --git a/node_modules/@agentmail/convex/src/component/_generated/server.d.ts b/node_modules/@agentmail/convex/src/component/_generated/server.d.ts
index bec05e6..c821c71 100644
--- a/node_modules/@agentmail/convex/src/component/_generated/server.d.ts
+++ b/node_modules/@agentmail/convex/src/component/_generated/server.d.ts
@@ -21,6 +21,12 @@ import {
 } from "convex/server";
 import type { DataModel } from "./dataModel.js";

+type Env = {
+  readonly AGENTMAIL_API_KEY: string | undefined;
+  readonly AGENTMAIL_WEBHOOK_SECRET: string | undefined;
+  readonly AGENTMAIL_BASE_URL: string | undefined;
+};
+
 /**
  * Define a query in this Convex app's public API.
  *
@@ -94,6 +100,7 @@ export declare const internalAction: ActionBuilder<DataModel, "internal">;
  * @returns The wrapped function. Import this function from `convex/http.js` and route it to hook it up.
  */
 export declare const httpAction: HttpActionBuilder;
+export declare const env: Env;

 /**
  * A set of services for use within Convex query functions.
diff --git a/node_modules/@agentmail/convex/src/component/_generated/server.js b/node_modules/@agentmail/convex/src/component/_generated/server.js
index bf3d25a..f7cad57 100644
--- a/node_modules/@agentmail/convex/src/component/_generated/server.js
+++ b/node_modules/@agentmail/convex/src/component/_generated/server.js
@@ -91,3 +91,4 @@ export const internalAction = internalActionGeneric;
  * @returns The wrapped function. Import this function from `convex/http.js` and route it to hook it up.
  */
 export const httpAction = httpActionGeneric;
+export const env = process.env;
diff --git a/node_modules/@agentmail/convex/src/component/convex.config.ts b/node_modules/@agentmail/convex/src/component/convex.config.ts
index b1cc7f1..fad0779 100644
--- a/node_modules/@agentmail/convex/src/component/convex.config.ts
+++ b/node_modules/@agentmail/convex/src/component/convex.config.ts
@@ -1,7 +1,14 @@
 import { defineComponent } from "convex/server";
+import { v } from "convex/values";
 import workpool from "@convex-dev/workpool/convex.config";

-const component = defineComponent("agentmail");
+const component = defineComponent("agentmail", {
+  env: {
+    AGENTMAIL_API_KEY: v.optional(v.string()),
+    AGENTMAIL_WEBHOOK_SECRET: v.optional(v.string()),
+    AGENTMAIL_BASE_URL: v.optional(v.string()),
+  },
+});
 component.use(workpool, { name: "sendPool" });
 component.use(workpool, { name: "callbackPool" });

diff --git a/node_modules/@agentmail/convex/src/component/utils.ts b/node_modules/@agentmail/convex/src/component/utils.ts
index e20117d..db619d2 100644
--- a/node_modules/@agentmail/convex/src/component/utils.ts
+++ b/node_modules/@agentmail/convex/src/component/utils.ts
@@ -1,3 +1,5 @@
+import { env } from "./_generated/server.js";
+
 const PERMANENT_STATUSES = new Set([
   400, 401, 404, 405, 410, 413, 414, 415, 422,
 ]);
@@ -8,6 +10,20 @@ const DEFAULT_BASE_URL = "https://api.agentmail.to/v0";
 // Convex document.
 const ERROR_BODY_LIMIT = 4096;

+function requireApiKey(): string {
+  const apiKey = env.AGENTMAIL_API_KEY?.trim();
+  if (!apiKey) {
+    throw new Error(
+      "AGENTMAIL_API_KEY is not set for the AgentMail component. " +
+        "Bind it through app.use(agentmail, {env: {...}}).",
+    );
+  }
+  if (apiKey.length > 512 || !/^[\x21-\x7e]+$/.test(apiKey)) {
+    throw new Error("AGENTMAIL_API_KEY contains invalid HTTP header characters.");
+  }
+  return apiKey;
+}
+
 /**
  * Parse an ISO timestamp into ms-since-epoch. Returns `Date.now()` on
  * malformed input — `Date.parse` returns `NaN` for bad strings, and `NaN`
@@ -40,17 +56,9 @@ export async function agentmailFetch(
   path: string,
   opts: FetchOptions,
 ): Promise<unknown> {
-  const apiKey = process.env.AGENTMAIL_API_KEY;
-  if (!apiKey) {
-    throw new Error(
-      "AGENTMAIL_API_KEY is not set on this Convex deployment. Run " +
-        "`npx convex env set AGENTMAIL_API_KEY <key>`.",
-    );
-  }
-  const baseUrl = (process.env.AGENTMAIL_BASE_URL ?? DEFAULT_BASE_URL).replace(
-    /\/$/,
-    "",
-  );
+  const apiKey = requireApiKey();
+  const configuredBaseUrl = env.AGENTMAIL_BASE_URL?.trim();
+  const baseUrl = (configuredBaseUrl || DEFAULT_BASE_URL).replace(/\/+$/, "");
   const url = new URL(baseUrl + path);
   if (opts.query) {
     for (const [k, v] of Object.entries(opts.query)) {
